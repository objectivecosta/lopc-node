var setup = require('../setup');
setup.database();

describe('UserController', function () {
  describe('#create', function () {
    it('should create an user when the request body is valid', function() {
      
    });
  });
});
